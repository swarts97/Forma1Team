@page
@model Forma1Teams.Pages.Teams.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Teams[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teams[0].YearOfFoundation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teams[0].WorldChampionNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teams[0].EntryFeePaid)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Teams) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.YearOfFoundation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorldChampionNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryFeePaid)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TeamID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TeamID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TeamID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
